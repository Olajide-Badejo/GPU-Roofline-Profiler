\section{Environment and raw tables}

The machine environment below is generated at report build time from the run
manifest, never typed from memory, so it always describes the machine that
produced the results in this document.

\inputgeneratedsmall{tables/environment_full.tex}

\subsection{Full timing table}

Every configuration the sweep ran, rather than the selection shown in the
results section.

\inputgeneratedsmall{tables/timing_full.tex}

\subsection{Full Nsight Compute metric table}

Every metric collected for every profiled kernel, as reported by Nsight Compute
and before any derivation. The metric names are the ones this version of the
tool actually exposes on this device, which is why they are recorded verbatim.

\inputgeneratedsmall{tables/ncu_full.tex}

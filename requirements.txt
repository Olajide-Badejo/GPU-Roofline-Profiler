# Python analysis and tooling dependencies.
#
# Pinned to the versions that actually install and pass on the target machine,
# which runs CPython 3.14. The versions I first pinned predated 3.14 and had no
# wheels for it, so pip tried to build pandas from source and failed for want of
# MSVC. These are the wheels that exist for 3.14; the pins reflect reality on
# this box rather than an aspiration.
numpy==2.5.1
pandas==3.0.3
matplotlib==3.11.1
pyyaml==6.0.3
pytest==9.1.1
tqdm==4.69.0
ruff==0.15.22
